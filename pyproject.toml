[project]
name = "hqew-crawler"
version = "0.1.0"
description = "Python implementation skeleton for HQEW daily crawler"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["crawler/tests"]
